---
name: execute-sql
description: To read the DB, use mcp-postgres MCP tools
disable-model-invocation: true
---
To read the DB, use `mcp-postgres` MCP tools.
If tools unavailable or timeout, ask developer to fix it and STOP.
No need to load schemas, always use the implicit one (set in the search_path).
