---
name: lint
description: For fast iterations use Cursor's read_lints tool as you refactor
disable-model-invocation: true
---
For fast iterations use Cursor's `read_lints` tool as you refactor.
Use `npm run lint:full:silent` to check compilation and linting errors at the end. It also fixes easy spaces/commas errors for you, don't waste time fixing them yourself. Don't `cd`, run all from root
