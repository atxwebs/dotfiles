Read `.cursor/commands/run_server.md` for how to run the server.
Use `mcp-graphql-local` tools to query the API running locally.
If available, use `mcp-graphql-dev` can be used to query the API running in development.
All queries/mutations must be named.
If MCP unavailable, ask developer to start it and STOP.
Find GraphQL types & queries with `.cursor/commands/read_symbol.md` instructions.