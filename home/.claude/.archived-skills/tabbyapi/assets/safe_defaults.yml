# Minimal "safe" fallback settings for most models, used when the frontend doesn't supply any settings
# with a request.

temperature:
  override: 0.8
  force: false

top_p:
  override: 0.9
  force: false

stop:
  override:
    - "<|eot_id|>"
    - "<|im_start|>"
    - "<|im_end|>"
    - "[/INST]"
    # - "</think>"
    # - "<think>"
  force: true

max_tokens:
  override: 1024
  force: false