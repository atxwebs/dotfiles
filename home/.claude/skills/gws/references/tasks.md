# Tasks

```bash
gws tasks tasklists list
gws tasks tasks list --params '{"tasklist":"TASKLIST_ID"}'
gws tasks tasks get --params '{"tasklist":"TASKLIST_ID", "task":"TASK_ID"}'
```
IDs from list output. tasklist = taskList id (e.g. MDU0NzM5...).
